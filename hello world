#include <stdio.h>
#include <string.h>
int main() 
{
    char str[100];
    gets(str);
	printf("Hello, World!\n");
  	printf("%s",str);
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
